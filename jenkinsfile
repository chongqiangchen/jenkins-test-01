pipeline {
     agent any

    triggers {
      pollSCM('H/2 * * * *')
    }

    tools {
      nodejs "nodejs12"
    }

    options {
      timeout(time: 10, unit: 'MINUTES')
    }

    stages{
      stage("install"){
        steps{
          sh "yarn install"
        }
      }
    }
}